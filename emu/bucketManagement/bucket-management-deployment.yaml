apiVersion: apps/v1
kind: Deployment
metadata:
  name: bucket-management
  labels:
    ellipsis-drive: bucket-management
spec:
  replicas: 1
  selector:
    matchLabels:
      ellipsis-drive: bucket-management
  template:
    metadata:
      labels:
        ellipsis-drive: bucket-management
    spec:
      serviceAccountName: s3-access-sa
      containers:
      - name: bucket-management
        image: ghcr.io/ellipsis-drive/bucket-management:latest
        resources:
          requests:
            cpu: 100m
        env:
          - name: POSTGRES_APP_PASS
            valueFrom:
              secretKeyRef:
                name: owl-db-password
                key: password
          - name: INTERNAL_MAIL
            valueFrom:
              secretKeyRef:
                name: internal-mail
                key: username
          - name: INTERNAL_MAIL_PASS
            valueFrom:
              secretKeyRef:
                name: internal-mail
                key: password
        envFrom:
          - configMapRef:
              name: ellipsis
        volumeMounts:
          - name: etmpfs
            mountPath: /mnt/etmpfs
      imagePullSecrets:
        - name: ghcr-secret

      volumes:
        - name: etmpfs
          persistentVolumeClaim:
            claimName: etmpfs